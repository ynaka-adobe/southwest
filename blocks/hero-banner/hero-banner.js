export default function decorate() {
  // hero-banner is a purely visual promo card driven entirely by CSS.
  // Two variants share the .hero-banner class and are distinguished by the
  // section wrapper class:
  //   - .banner-promo-container .hero-banner  → promo over the navy booking section
  //   - .hero-banner (default)                → Rapid Rewards card on light background
  // No JS behavior is required; EDS default-content decoration handles the
  // heading, list, and button-container structure.
}
