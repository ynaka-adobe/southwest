kind: "EdgeFunctions"
version: "1"
data:
  functions:
    - name: southwest-geo
  # Uncomment to enable secrets
  # secrets:
  #   - key: API_TOKEN
  #     value: ${{ API_TOKEN_SECRET }}
  # Uncomment to enable a KV store
  # kvs: true
